services:
  webapi:
    build:
      context: ..
      dockerfile: src/Handoff.WebApi/Dockerfile
    image: handoff-webapi:latest
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
